A diferença é que no exemplo do filtro, ele tratava o objeto em sequencia, ou seja, um conjunto de modificações
para o filtro. Já no exemplo do banco, ele utilizava apenas uma "seleção" de objeto. Além disso, ele utiliza uma fábrica
no exemplo do filtro.
A semelhança é que ambos utilizam uma lista encadeada para adicionar novos elementos de seleção. Seja um novo componente
do filtro ou um novo banco.